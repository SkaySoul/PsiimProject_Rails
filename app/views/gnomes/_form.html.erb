<%= form_with(model: gnome) do |form| %>
  <% if gnome.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(gnome.errors.count, "error") %> prohibited this gnome from being saved:</h2>

      <ul>
        <% gnome.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
