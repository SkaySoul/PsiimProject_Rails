<%= form_with(model: usergnome) do |form| %>
  <% if usergnome.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(usergnome.errors.count, "error") %> prohibited this usergnome from being saved:</h2>

      <ul>
        <% usergnome.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
